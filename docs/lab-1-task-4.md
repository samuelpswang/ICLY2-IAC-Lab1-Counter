# Task 4 Log

## Task

**Basic Info**

* Date: 24/10/2022
* Author: Samuel Wang
* Objective: 
    * Implement a top-level design which includes a binary-coded decimal (BCD) converter. (Completed)

**Result**

The top-level design `top.sv` included the two module designs: `counter.sv` and `bin2bcd.sv`. The signal `count` was passed as an internal signal to the `bin2bcd.sv` module. The output `BCD` signal was then passed to vBuddy to be displayed.

The designed built succesfully and was simulated on the vBuddy. The simulation behaved as expected.
